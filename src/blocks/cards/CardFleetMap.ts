import type { Block } from "payload";

/** Fleet location map card — content comes from the Cruise Map global.
 * → components/molecules/CardFleetMap.tsx */
export const CardFleetMapBlock: Block = {
  slug: "cardFleetMap",
  interfaceName: "CardFleetMapBlock",
  labels: { singular: "Card: Fleet Map", plural: "Cards: Fleet Map" },
  fields: [],
};
